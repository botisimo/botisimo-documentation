$(usernameplain)
================

Resolves the command issuer's username as plain text.

Usage
    $(usernameplain)

Example
    ::

        Hi, $(usernameplain)!

    ``!hi`` -> twitch/mixer/youtube -> ``Hi, bob!``

    ``!hi`` -> discord -> ``Hi, bob!``
