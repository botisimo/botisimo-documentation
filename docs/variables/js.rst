$(js)
=====

Resolves the value of a javascript expression

Usage
    $(js ``<javascript>``)

Example
    ::

        $(js ['need', 'a', 'better', 'example'].join(' '))

    ``!js`` -> ``need a better example``
